# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v54.0.0/ before
writing any code — **v54**, matching `package.json` (`expo: ~54.0.0`) and the
lockfile (54.0.35), not whatever `latest` redirects to.

The version here is pinned to the one actually installed. If the SDK is
upgraded, change this line in the same commit; a doc pointing at a version the
app isn't on is worse than no doc, because the APIs it describes compile
against a different runtime.
