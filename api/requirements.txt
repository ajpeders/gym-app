fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy==2.0.36
pydantic==2.10.4
pydantic-settings==2.7.1
email-validator==2.2.0
PyJWT==2.10.1
passlib==1.7.4
bcrypt==4.0.1
httpx==0.28.1
anthropic>=0.49.0
# Reusable LLM-companion extension (its own repo). Provides the provider seam
# (Ollama/Claude) and the tool-calling coach at /api/companion. Needs `git` in
# the build image (see Dockerfile).
companion @ git+https://git.thelunadog.com/openclaw/companion.git@v0.2.0
pytest==8.3.4
